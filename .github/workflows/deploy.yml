name: Coupon - Build and Deploy

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. 코드 체크아웃
      - name: Checkout code
        uses: actions/checkout@v3

      # 2. JDK 21 설정
      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      # 3. Maven 빌드
      - name: Build with Maven
        run: |
          chmod +x ./mvnw
          ./mvnw clean package -DskipTests

      # 4. Docker 로그인
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 5. Docker 이미지 빌드 및 푸시
      - name: Docker Coupon image
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./Dockerfile
          push: true
          # ★ 이미지 이름을 'shop'으로 변경
          tags: ${{ secrets.DOCKER_USERNAME }}/coupon:latest

      # 6. 서버 배포
      - name: Deploy to Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          port: 8822
          script_stop: true
          script: |
            set -e
            
            echo "🔄 Shop 배포 시작..."
            
            # 네트워크 보장
            docker network create my-network || true
            
            # 기존 컨테이너 정리 (이름: shop)
            if [ "$(docker ps -aq -f name=shop)" ]; then
                echo "🧹 기존 Shop 컨테이너 삭제..."
                docker rm -f shop
            fi
            
            # 이미지 Pull
            docker pull ${{ secrets.DOCKER_USERNAME }}/shop:latest
            
            # ★ Shop 실행
            # - 포트: 없음 (외부 노출 X, Gateway 통해서만 접근)
            # - 이름: shop
            # - 네트워크: my-network
            echo "🚀 Shop 실행 중..."
            docker run -d \
              --name shop \
              --network my-network \
              --restart unless-stopped \
              -e EUREKA_CLIENT_SERVICEURL_DEFAULTZONE="http://eureka:8761/eureka/" \
              ${{ secrets.DOCKER_USERNAME }}/shop:latest
            
            # 실행 확인
            sleep 5
            docker logs --tail 20 shop